function minimax(node, depth, maximizingPlayers)
    if depth = 0 or node is a terminal
        return the heuristic value of node
    if maximizingPlayer //Minimo de los máximos
        bestValue:= MINUS_INFINITY
        for each childof node
            v := minimax(child, depth-1,FALSE)
            bestValue:=max(bestValue,v)
        return bestValue
    else
        bestValue:=INFINITY
        for each child of node
            v := minimax(child, depth-1, TRUE)
            bestValue:=min()
        return bestValue
